# ============================================
# AI Art Revenue Exchange - Production Environment Example
# Copy this file to .env and configure with your production values
# ============================================

# Application
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# Database (Production PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://aiart_user:STRONG_PASSWORD_HERE@postgres:5432/ai_art_exchange?schema=public
DB_USER=aiart_user
DB_PASSWORD=STRONG_PASSWORD_HERE
DB_NAME=ai_art_exchange

# Redis Cache (Production Redis)
REDIS_URL=redis://:STRONG_REDIS_PASSWORD@redis:6379
REDIS_PASSWORD=STRONG_REDIS_PASSWORD_MIN_16_CHARS

# Security - MUST GENERATE UNIQUE VALUES FOR PRODUCTION
# Use: openssl rand -base64 64 | tr -d '\n'
JWT_SECRET=GENERATE_UNIQUE_SECRET_MIN_64_CHARS_HERE
JWT_REFRESH_SECRET=GENERATE_UNIQUE_SECRET_MIN_64_CHARS_HERE
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d
ENCRYPTION_KEY=GENERATE_UNIQUE_KEY_MIN_64_CHARS_HERE
COOKIE_SECRET=GENERATE_UNIQUE_SECRET_MIN_64_CHARS_HERE

# CORS - Production Domains Only
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Rate Limiting
THROTTLE_TTL=60000
THROTTLE_LIMIT=100

# AWS S3 (Production)
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY
AWS_S3_BUCKET=your-production-s3-bucket
AWS_REGION=us-east-1

# Stripe (Production Keys - NOT Test Keys)
STRIPE_SECRET_KEY=sk_live_YOUR_LIVE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET
STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_LIVE_PUBLISHABLE_KEY
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_LIVE_PUBLISHABLE_KEY

# AI Services - Local Ollama + Stable Diffusion (No API Keys Required)
# Ollama Configuration
AI_API_URL=http://localhost:11434
AI_API_KEY=not-required-for-ollama
OLLAMA_MODEL=llava

# Stable Diffusion WebUI Configuration
SD_API_URL=http://localhost:7860
SD_DEFAULT_STEPS=30
SD_DEFAULT_CFG_SCALE=7
SD_DEFAULT_SAMPLER=DPM++ 2M Karras

# Admin Configuration
# Generate admin wallet UUID: node -e "console.log(require('crypto').randomUUID())"
ADMIN_WALLET_ID=00000000-0000-0000-0000-000000000000
ADMIN_EMAIL=admin@yourdomain.com
# Generate hash: node -e "console.log(require('bcrypt').hashSync('YOUR_ADMIN_PASSWORD', 12))"
ADMIN_PASSWORD_HASH=$2b$12$REPLACE_WITH_ACTUAL_BCRYPT_HASH

# Platform Settings
PLATFORM_FEE_PERCENT=10
ADMIN_CLAIM_RATIO_ENABLED=5
ADMIN_CLAIM_RATIO_DISABLED=2

# Mature Content
MATURE_CONTENT_ENABLED=true
AGE_VERIFICATION_REQUIRED=true

# Logging
LOG_LEVEL=warn
LOG_FILE_PATH=/var/log/ai-art-exchange

# Monitoring (Optional)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Feature Flags
ENABLE_DATA_MONETIZATION=true
ENABLE_ADS=true
ENABLE_REFERRALS=true
ENABLE_CHALLENGES=true

# Frontend Configuration
NEXT_PUBLIC_API_URL=https://yourdomain.com/api/v1

# ============================================
# PRODUCTION SECURITY CHECKLIST
# ============================================
# [ ] All secrets are unique and randomly generated (64+ chars)
# [ ] Different from development/staging environments
# [ ] DATABASE_URL uses strong password (16+ chars)
# [ ] REDIS_PASSWORD is strong (16+ chars)
# [ ] Stripe uses LIVE keys (sk_live_*, pk_live_*)
# [ ] CORS only allows production domains
# [ ] Admin password is hashed with bcrypt
# [ ] SSL certificates installed in nginx/ssl/
# [ ] Ollama is running: ollama serve
# [ ] Ollama model downloaded: ollama pull llava
# [ ] Stable Diffusion WebUI running: ./webui.sh --api --listen
# [ ] Firewall configured (ports 22, 80, 443 only)
# [ ] Backup strategy implemented
# [ ] Monitoring configured
